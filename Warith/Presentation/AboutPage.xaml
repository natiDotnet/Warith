<Page x:Class="Warith.Presentation.AboutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Warith.Presentation"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:toolkit="using:Uno.Toolkit.UI"
      Background="{ThemeResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <toolkit:NavigationBar x:Name="NavBar" Grid.Row="0">
            <toolkit:NavigationBar.Content>
                <Image Source="ms-appx:///Assets/Splash/favicon.png"
                       HorizontalAlignment="Left"
                       Width="128"
                       Height="40" />
            </toolkit:NavigationBar.Content>
            <toolkit:NavigationBar.PrimaryCommands>
                <AppBarButton Label="Home" uen:Navigation.Request="!Main">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Home" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="About" uen:Navigation.Request="!About">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Help" />
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Label="Contact" uen:Navigation.Request="!Contact">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Contact" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </toolkit:NavigationBar.PrimaryCommands>
        </toolkit:NavigationBar>

        <ScrollViewer Grid.Row="1">
            <StackPanel Padding="20" Spacing="20">
                <TextBlock Text="Warith is a professional Islamic inheritance calculator designed to help users accurately distribute estates according to Sharia law." 
                           TextWrapping="Wrap" FontSize="16"/>
                <TextBlock Text="Features:" FontWeight="SemiBold" FontSize="18"/>
                <StackPanel Margin="10,0,0,0">
                    <TextBlock Text="• Support for all major Islamic schools of thought (Madhaheb)." />
                    <TextBlock Text="• Handles complex cases like debts, wills, and predeceased relatives." />
                    <TextBlock Text="• Responsive design for mobile and desktop use." />
                </StackPanel>
                <TextBlock Text="Version 1.0.0" Foreground="Gray" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
